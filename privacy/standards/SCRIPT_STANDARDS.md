# Signal Labs Script Standards

Before script work, review this file.

- Whole files only.
- Avoid silent failures.
- Preserve calculator math unless the release explicitly changes it.
- Shared components should live under `/assets/components/`.
- Future shared reports should live under `/assets/reports/`.
