# PHP/MySQL Starter Plan — Superseded by v0.99.1

This document is retained for reference only.

As of v0.99.1, Signal Schedule's first backend target is Cloudflare-native: Pages, Workers/Pages Functions, D1, KV, R2, and Secrets.

PHP/MySQL remains a possible future adapter if the product later needs traditional hosting, but it is not the default implementation path. Rule 24 requires backend portability so this option remains open.
