# Signal Schedule v1.0.0 Database Planning

The v1.x path starts with a static JSON data layer, then moves to repository/adapters, Workers/Pages Functions, and D1. PHP/MySQL remains a possible future adapter, not the first implementation target.
