## v0.17.1 UI Debt Audit Rules

- Store facts, not assumptions.
- Engines consume facts and produce explainable outcomes.
- Foundation releases should not add new preview panels unless the UI is the purpose of the release.
- Every registry entry must resolve to a defined render function before packaging.
