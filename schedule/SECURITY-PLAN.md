# Signal Schedule Security Plan — v1.0.0

No authentication, credentials, or write APIs are included yet.

Future Cloudflare releases should use Secrets for credentials, role-aware endpoints, and audit logging before live writes.
