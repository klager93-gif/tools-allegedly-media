## v1.0.0 Data Persistence Plan

v1.0.0 does not create a live database. It creates the first static data boundary using JSON.

The current source is:

```text
JSON adapter
```

The planned future source is:

```text
Cloudflare D1 adapter
```

Rule 24 requires adapters to remain replaceable.
