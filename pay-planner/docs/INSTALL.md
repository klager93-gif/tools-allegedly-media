# Install Notes

## Home v0.9.5 — Restore Missing Tools

This is a recovery documentation package.

It does not contain the restored `/overtime/` and `/timeoff/` folders. Those folders should be copied from the user's backup and committed with these documentation updates.
