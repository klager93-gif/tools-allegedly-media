# Signal Labs

Useful tools without the noise.

## Current Recovery Release

**Home v0.9.5 — Restore Missing Tools**

This recovery package documents the restoration of the missing `/overtime/` and `/timeoff/` folders after they were accidentally omitted from full replacement release ZIPs.

Restore the folders from:

```text
2026-06-10-before-home-v0.9.2-paycheck-v1.0.0-root-cleanup-stable-paycheck
```

Folders to restore:

```text
/overtime/
/timeoff/
```

## Current Repository Organization

Root entry files remain at the root.

Operational documents live in:

```text
/docs/
```

Build metadata lives in:

```text
/build/
```

Standards live in:

```text
/standards/
```
